plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    // JUnit 5
    testImplementation 'org.junit.jupiter:junit-jupiter:5.11.0'
}

test {
    useJUnitPlatform() // ensures JUnit 5 tests run
    testLogging {
        events "passed", "skipped", "failed"
    }
}